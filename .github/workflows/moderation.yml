name: moderation

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions: {}

jobs:
  spam-detection:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      issues: write
      models: read
      pull-requests: write

    steps:

      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
          show-progress: false

      - uses: github/ai-moderator@6cac571a38f77b59fbe85232dc4e849d4a8ec302 # v1.1.3
        with:
          ai-label: 'ai-generated'
          minimize-detected-comments: true
          spam-label: 'spam'
          token: ${{ secrets.GITHUB_TOKEN }}
